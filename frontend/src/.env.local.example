# Leave empty to use Next.js proxy rewrites (recommended for docker-compose)
NEXT_PUBLIC_API_URL=
# Internal URL used by Next.js server-side rewrites (server → backend)
INTERNAL_API_URL=http://localhost:8000
