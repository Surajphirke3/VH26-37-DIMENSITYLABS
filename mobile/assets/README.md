# Assets

Place the following image assets in this directory before building:

- `icon.png` — App icon (1024x1024 px recommended)
- `splash.png` — Splash screen image (1284x2778 px recommended)

These are referenced in `app.json` under the `expo.icon` and `expo.splash.image` fields.
